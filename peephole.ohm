peephole {
  main = item+
  item =
    | "_save " spaces "(_temps, " inneritem+ ")" -- save
    | any -- other
  inneritem =
    | "_save " spaces "(_temps, " inneritem+ ")" -- save
    | ~")" any -- other
}
